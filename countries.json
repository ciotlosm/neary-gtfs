{
  "_comment": "Single source of truth for which feeds we publish. 'include[]' lists Transitous source names (from <iso>.json). Each becomes either a plain mirror, OR — if a feeds/<id>/config.json declares enhances:'<name>' — an enhanced build atop the Transitous zip.",
  "countries": ["ro"],
  "include": ["Cluj-Napoca", "Bucuresti-Ilfov"]
}
